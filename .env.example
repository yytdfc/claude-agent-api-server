# Claude Agent API Server - Environment Variables

# ============================================================================
# Claude API Configuration
# ============================================================================

# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Default Claude model
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Default background model (for agents)
# ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-3-5-haiku-20241022

# ============================================================================
# AWS Bedrock Configuration (Optional)
# ============================================================================

# Set to 1 to use AWS Bedrock instead of Anthropic API
# CLAUDE_CODE_USE_BEDROCK=0

# AWS credentials (if using Bedrock)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-west-2

# ============================================================================
# LiteLLM Proxy Configuration (Optional)
# ============================================================================

# Base URL for LiteLLM proxy (if using proxy mode)
# ANTHROPIC_BASE_URL=http://localhost:8000/v1/messages

# ============================================================================
# S3 Workspace Sync Configuration
# ============================================================================

# S3 bucket name for workspace storage (required for workspace sync)
S3_WORKSPACE_BUCKET=my-workspace-bucket

# S3 key prefix for user data (default: "user_data")
S3_WORKSPACE_PREFIX=user_data

# Local base path for workspaces (default: "/workspace")
WORKSPACE_BASE_PATH=/workspace

# ============================================================================
# Claude Directory Backup Configuration
# ============================================================================

# Backup interval for .claude directory in minutes (default: 5)
# The .claude directory contains session history and is automatically
# backed up to S3 at this interval for all active users
CLAUDE_BACKUP_INTERVAL_MINUTES=5

# ============================================================================
# AWS Cognito Configuration (Optional - for authentication)
# ============================================================================

# Cognito User Pool ID
# COGNITO_USER_POOL_ID=us-west-2_xxxxxxxxx

# Cognito App Client ID
# COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# AWS Region for Cognito
# COGNITO_REGION=us-west-2

# ============================================================================
# Server Configuration
# ============================================================================

# Server host (default: 127.0.0.1)
# HOST=0.0.0.0

# Server port (default: 8000)
# PORT=8000

# Log level (default: info)
# LOG_LEVEL=info

# ============================================================================
# gRPC Server Configuration (Optional - for HTTP/2 bidirectional streaming)
# ============================================================================

# Enable gRPC server for terminal bidirectional streaming
# Requires Envoy proxy for browser access (see envoy.yaml)
# ENABLE_GRPC_SERVER=false

# gRPC server port (default: 50051)
# GRPC_PORT=50051

# ============================================================================
# Observability (Optional)
# ============================================================================

# Langfuse configuration (if using Langfuse for observability)
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_HOST=https://cloud.langfuse.com
